# План реализации мягкого фона фотокарусели

> **Для исполнителя:** ОБЯЗАТЕЛЬНЫЙ ПОДНАВЫК: использовать `superpowers:executing-plans` для пошагового выполнения этого плана. Шаги отслеживаются флажками `- [ ]`.

**Цель:** заменить броскую почти чёрную подложку фотокарусели на мягкое размытое продолжение активной фотографии, сохранив показ исходного кадра целиком.

**Архитектура:** `PlacePhotoGallery` продолжает владеть всем визуальным состоянием галереи. Перед основным `motion.img` добавляется декоративный CSS-фон активного изображения с `background-size: cover`, увеличением, размытием и тёплой вуалью; основной кадр остаётся единственным `<img>` в DOM и использует `object-contain`, а существующее поведение навигации не меняется.

**Технологии:** React 19, TypeScript, Tailwind CSS v4, Motion, Vite.

---

### Задача 1: Добавить мягкую фотоподложку

**Файлы:**
- Изменить: `src/components/place-details/PlacePhotoGallery.tsx:77-165`

- [x] **Шаг 1: Заменить чёрную подложку и добавить декоративный слой**

В `PlacePhotoGallery` оставить тёпло-коричневый fallback на контейнере, добавить перед `motion.img` декоративный CSS-фон активной фотографии и тёплую вуаль:

```tsx
<section className="relative h-[min(65dvh,520px)] overflow-hidden bg-[var(--color-text-secondary)] ...">
  <div
    className="pointer-events-none absolute inset-0 scale-110 bg-cover bg-center saturate-[0.72] blur-2xl"
    style={{ backgroundImage: `url(${JSON.stringify(activePhotoSrc)})` }}
  />
  <div className="pointer-events-none absolute inset-0 bg-[rgba(50,34,22,0.32)]" />

  <motion.img
    className={
      imageStatus === "error"
        ? "relative z-1 block h-full w-full object-contain opacity-0"
        : "relative z-1 block h-full w-full object-contain"
    }
    ...
  />
</section>
```

Поднять существующие error state, нижний градиент, навигационные кнопки, счётчик, закрытие и заголовок выше основного изображения через `z-2`/`z-3`. Не менять обработчики, высоту галереи, `object-contain`, подписи или анимацию активного кадра.

- [x] **Шаг 2: Проверить TypeScript**

Запустить:

```bash
pnpm typecheck
```

Ожидаемый результат: команда завершается с кодом `0`, ошибок TypeScript нет.

### Задача 2: Синхронизировать документацию текущей фичи

**Файлы:**
- Изменить: `specs/001-kursk-places-map/spec.md:208`
- Изменить: `specs/001-kursk-places-map/plan.md:229-237`
- Изменить: `specs/001-kursk-places-map/tasks.md` после Phase 34

- [x] **Шаг 1: Уточнить требование FR-039**

Указать, что свободные поля `object-contain` заполняются размытым и приглушённым продолжением активного кадра с тёплым fallback, а не чистым чёрным фоном.

- [x] **Шаг 2: Обновить технический подход**

В дополнении про единый фотоблок описать декоративную копию активной фотографии через `object-cover`, размытие, приглушение насыщенности и тёплую вуаль. Зафиксировать повторное использование того же URL без отдельного медиа-ресурса.

- [x] **Шаг 3: Добавить выполненную задачу**

Добавить следующую Phase и выполненную задачу `T187` про мягкую фотоподложку. В проверке перечислить `pnpm typecheck`, `pnpm build`, `pnpm build-storybook` и ручную visual QA; отметить отсутствие нового автоматизированного покрытия по правилу проекта.

### Задача 3: Проверить сборку и интерфейс

**Файлы:**
- Изменить: `src/components/place-details/PlaceDetailsPanel.stories.tsx`

- [x] **Шаг 1: Собрать приложение и Storybook**

Запустить:

```bash
pnpm build
pnpm build-storybook
```

Ожидаемый результат: обе команды завершаются с кодом `0`; существующие предупреждения, если появятся, не связаны с новой подложкой.

- [x] **Шаг 2: Выполнить visual QA на desktop**

Открыть `/maps/dozapravka?place=1465` при ширине desktop. Проверить, что вертикальный кадр виден целиком, боковые поля заполнены мягким размытым продолжением, заголовок и все overlay-кнопки читаемы, а основной контент панели не сдвинулся.

- [x] **Шаг 3: Выполнить visual QA на mobile**

Повторить проверку при размере `375×812`. Убедиться, что размытый фон не выходит за границы панели, фотография не обрезана, кнопка закрытия учитывает safe area и горизонтальная прокрутка страницы не появилась.

- [x] **Шаг 4: Проверить горизонтальный кадр и fallback**

Открыть место с горизонтальным изображением и убедиться, что подложка остаётся тихой и не ухудшает читаемость. Добавить story `PhotoLoadError` с отсутствующим локальным изображением и проверить её в Storybook: вместо чёрного поля должна оставаться тёпло-коричневая подложка и сообщение об ошибке.

- [x] **Шаг 5: Зафиксировать реализацию**

```bash
git add src/components/place-details/PlacePhotoGallery.tsx \
  specs/001-kursk-places-map/spec.md \
  specs/001-kursk-places-map/plan.md \
  specs/001-kursk-places-map/tasks.md \
  docs/superpowers/plans/2026-06-29-carousel-background.md
git commit -m "Смягчить фон фотокарусели"
```
